# 📋 プリント刺繍加工業向け案件管理システム

Print & Embroidery Project Management System

ローカルネットワーク内の複数デバイス（PC・iPad・スマホ）からアクセスできる案件管理Webアプリです。

## ✨ 主な機能

- **3つのビュー**
  - 📊 **一覧ビュー**: 全案件を表で表示。ステータス・担当者・加工種別で絞り込み。納期順ソート。
  - 🎯 **カンバンビュー**: ステータスごとに列を分けて表示。ドラッグ&ドロップでステータス更新。
  - 📅 **カレンダービュー**: 納期を月カレンダーに表示。案件をクリックで詳細表示。

- **案件管理**
  - 案件の作成・閲覧・編集・削除
  - 案件名・顧客名・納期・加工種別・数量・担当者などを管理
  - 優先度・ステータス・メモ・関連資料リンクなどを保存

- **担当者管理**
  - 担当者情報の追加・編集・削除
  - 1日の稼働可能時間を設定
  - フルタイム・パート・生産管理・デザイナーなど職種を分類

- **コピペ取り込み機能**
  - LINE・メール・チャットのテキストを貼り付け
  - 案件名・顧客名・数量・納期などを自動抽出
  - 確認・修正後に登録

- **作業予定時間集計**
  - 担当者ごとの当日の作業予定時間を表示
  - 稼働可能時間を超えているか視覚的に判定

## 🚀 クイックスタート

### 1. インストール

```bash
npm install
```

### 2. サーバーの起動

```bash
npm start
```

以下のようなメッセージが表示されます：

```
╔════════════════════════════════════════════════════════════╗
║  プリント刺繍加工業向け案件管理システム                    ║
║  Production Management System                              ║
╚════════════════════════════════════════════════════════════╝

✓ サーバーが起動しました

📌 ローカルアクセス:  http://localhost:3000
📌 LANアクセス:       http://192.168.x.x:3000

💡 複数デバイスからは上記のLAN IPアドレスでアクセスしてください
```

### 3. ブラウザでアクセス

#### ローカルアクセス（同じPC）
```
http://localhost:3000
```

#### LAN内の他のデバイス（iPad・スマホ）
サーバーのLAN IPアドレスを確認して、以下のようにアクセス：
```
http://192.168.x.x:3000
```

LAN IPアドレスは、サーバー起動時に表示されます。

## 📊 ステータスの流れ

```
受注前 → 受注確定 → 生産待ち → 生産中 → 検品 → 納品済
```

各ステータスはカンバンビューで視覚的に管理でき、ドラッグ&ドロップで更新できます。

## 💾 データ保存

すべてのデータは同じディレクトリの `db/projects.db` というSQLiteファイルに保存されます。

### ファイル構成

```
Production_Management/
├── db/
│   ├── projects.db       ← データベースファイル
│   ├── schema.sql        ← スキーマ定義
│   └── init.js           ← 初期化スクリプト
├── public/
│   ├── index.html        ← メインページ
│   ├── js/
│   │   ├── app.js        ← UIロジック
│   │   ├── api.js        ← API層
│   │   └── utils.js      ← ユーティリティ
│   └── styles/
│       └── main.css      ← スタイル
├── server.js             ← Expressサーバー
├── package.json
└── README.md
```

## 🎯 使い方

### 新規案件の登録

#### 方法1: フォームから直接入力
1. 「➕ 新規案件」ボタンをクリック
2. 案件情報を入力
3. 「保存」をクリック

#### 方法2: コピペ取り込み機能
1. 「📥 コピペ取り込み」タブを開く
2. LINE・メール等のテキストを貼り付け
3. 「🔍 テキストから抽出」をクリック
4. 自動抽出された情報を確認・修正
5. 「登録」をクリック

### 案件の編集・削除

#### 一覧ビュー
- 「✎ 編集」をクリックしてモーダルを開く
- 情報を修正して「保存」
- 「削除」で案件削除

#### カンバンビュー
- カードをドラッグ&ドロップでステータス変更
- 「✎ 編集」で詳細情報を修正

### 担当者管理

1. 「👥 担当者管理」をクリック
2. 「➕ 新規担当者」で追加、「✎ 編集」で修正
3. 「削除」でスタッフを削除

初回起動時はサンプルとして3名(社長・三浦・鈴木)が登録されます。
名前や勤務時間・得意加工(スキルタグ)は「従業員管理」画面から随時編集できます。

## 📱 マルチデバイス対応

このアプリは**モバイルレスポンシブ対応**です：

- **PC**: 全機能をフルに使用可能
- **iPad**: 表・カンバン・カレンダーをタブレットサイズで表示
- **スマホ**: コンパクト表示に自動調整。横向き表示推奨

LAN内の全デバイスから同じサーバーにアクセスするため、複数人での同時編集も可能です。

## 🔧 技術スタック

| 項目 | 仕様 |
|------|------|
| **バックエンド** | Node.js + Express.js |
| **データベース** | SQLite (better-sqlite3) |
| **フロントエンド** | HTML + CSS + Vanilla JavaScript |
| **ネットワーク** | 0.0.0.0でリッスン（LAN内マルチデバイス対応） |

## 📈 スケーラビリティ

- **設計想定**: 月100件、年900件程度の案件規模
- **DB最適化**: インデックスを活用した高速クエリ
- **フロントエンド**: ビルド不要で軽量。ブラウザで直接実行可能

## 📝 API リファレンス

### 案件API

```http
GET    /api/projects          # 全案件取得
GET    /api/projects/:id      # 単一案件取得
POST   /api/projects          # 案件作成
PUT    /api/projects/:id      # 案件更新
DELETE /api/projects/:id      # 案件削除
```

### 担当者API

```http
GET    /api/staff             # 全担当者取得
POST   /api/staff             # 担当者作成
PUT    /api/staff/:id         # 担当者更新
DELETE /api/staff/:id         # 担当者削除（ソフトデリート）
```

### 統計API

```http
GET    /api/stats/daily-workload?date=YYYY-MM-DD  # 担当者別作業時間
```

## 🛠️ トラブルシューティング

### ポート3000が既に使用されている場合

```bash
PORT=4000 npm start
```

### DBファイルをリセットしたい(開発機のみ)

```bash
rm db/projects.db
npm start
```

初回起動時と同じように初期化されます。
**⚠️ 本番機では絶対に実行しないこと(全業務データが消えます)。本番でDBに問題が起きた場合は次の「バックアップからの復元」を行う。**

### データベースが破損した場合(バックアップからの復元)

自動バックアップがサーバー稼働中に1日1回 `db/backups/projects_auto_日付.db` として保存されている
(30世代保持。`.env` の `DB_BACKUP_EXTRA_DIR` 設定時はNASにも二重保存)。
また update.bat 実行時にも更新前バックアップ(`projects_manual_日時.db`)が作られる。

復元手順(本番機):

1. サーバーを停止する(サービス化済みなら `net stop HiBoard`、そうでなければ node のウィンドウを閉じる)
2. 壊れた `db\projects.db` を `db\projects_broken_日付.db` など別名に変更して退避する(削除しない)
3. `db\backups\` から**最新の日付**のバックアップファイルをコピーし、`db\projects.db` という名前で `db\` 直下に置く
4. サーバーを起動する(`net start HiBoard` または update.bat)
5. ブラウザで画面を開き、案件一覧・スケジュールボードが表示されることを確認する

※ 復元した場合、バックアップ取得時刻以降に入力されたデータ(最大1日分)は失われる。
　当日の受注候補・作業実績は各担当者に確認して再入力する。

## 📄 ライセンス

ISC

## 🤝 サポート

問題が発生した場合は、ターミナルに出力されるエラーメッセージを確認してください。

## 🎓 今後の拡張機能案

- [ ] ユーザー認証（複数ユーザーの権限管理）
- [ ] 工程進捗の細分化（複数工程の追跡）
- [ ] レポート機能（月別集計、スタッフ別実績）
- [ ] 自動通知（納期間近案件）
- [ ] 画像アップロード（参考資料の保存）
- [ ] CSVエクスポート（データ分析用）
- [ ] 外部連携（Slack通知など）

---

**最終更新**: 2026年6月24日  
**バージョン**: 1.0.0
